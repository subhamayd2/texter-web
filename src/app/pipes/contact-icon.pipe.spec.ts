import { ContactIconPipe } from './contact-icon.pipe';

describe('ContactIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactIconPipe();
    expect(pipe).toBeTruthy();
  });
});
